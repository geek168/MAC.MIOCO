<Window x:Class="MAC.MIOCO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MAC.MIOCO"
        xmlns:w="clr-namespace:MAC.MIOCO"
        ResizeMode="NoResize"
        Background="BlanchedAlmond"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="MAC.MIOCO" Height="700" Width="1000">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <TextBlock FontSize="40">MAC.MIOCO 管理系统</TextBlock>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock FontSize="20" Width="80">账号：</TextBlock>
            <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" FontSize="20" Width="300">
                <Validation.ErrorTemplate>
                    <ControlTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Placeholder for the TextBox itself -->
                            <Border BorderBrush="Red" BorderThickness="1">
                                <AdornedElementPlaceholder x:Name="textBox"/>
                            </Border>
                            <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" VerticalAlignment="Center" Margin="5 0 0 0 "/>
                        </StackPanel>
                    </ControlTemplate>
                </Validation.ErrorTemplate>
            </TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock FontSize="20" Width="80">密码：</TextBlock>
            <PasswordBox w:PasswordHelper.Attach="True" 
                w:PasswordHelper.Password="{Binding Password, Mode=TwoWay}"
                FontSize="20" Width="300"></PasswordBox>
        </StackPanel>
        <StackPanel>
            <Button Command="{Binding ClickCommand}" Width="100" FontSize="20" Background="LightBlue">登录</Button>
        </StackPanel>
    </StackPanel>
</Window>
